import React from 'react';

const MessageBox = () => {
    return (
        <div id={'message-box'}>
            Here are the Message box displayed
        </div>
    );
};

export default MessageBox;
